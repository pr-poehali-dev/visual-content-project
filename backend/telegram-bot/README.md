# Telegram Bot - Vizi Stickers

Телеграм-бот для приёма заявок на стикеры и AI-фотосессии.

## 🚀 Быстрый старт

### 1. Проверьте секреты проекта

Убедитесь, что в проекте настроены все необходимые секреты:
- `TELEGRAM_BOT_TOKEN` - токен от @BotFather
- `ADMIN_TELEGRAM_ID` - ваш Telegram ID для уведомлений
- `SMTP_EMAIL` - email для уведомлений (vizi-stickers@mail.ru)
- `SMTP_PASSWORD` - пароль приложения Mail.ru

### 2. Настройте webhook

Webhook уже должен быть настроен автоматически при деплое, но если нужно настроить вручную:

```bash
cd backend/telegram-bot
python3 setup-webhook.py
```

Или настройте webhook вручную через Telegram API:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://functions.poehali.dev/6a33fdc5-867a-4789-92e3-561eb84d9f05"}'
```

### 3. Проверьте работу бота

1. Найдите бота в Telegram (username указан в токене)
2. Отправьте команду `/start`
3. Должно появиться приветственное сообщение с кнопками

## 📋 Команды бота

- `/start` - Главное меню с кнопками
- `/stickers` - Информация о стикерах
- `/photoshoot` - Информация о фотосессиях
- `/price` - Прайс-лист
- `/portfolio` - Примеры работ
- `/contact` - Контакты

## 🔧 Функционал

### Интерактивные кнопки
- 🚀 Начать общение
- 🎨 Брендовые стикеры
- 📸 AI-фотосессии
- 💰 Прайс-лист
- ✨ Примеры работ
- 📞 Связаться с нами

### Уведомления админу
Когда пользователь отправляет заявку:
1. Админ получает уведомление в Telegram
2. Админ получает email на vizi-stickers@mail.ru

## 🐛 Отладка

### Проверка webhook
```bash
curl "https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo"
```

### Удаление webhook (для тестирования)
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_TOKEN>/deleteWebhook"
```

### Получение обновлений вручную (без webhook)
```bash
curl "https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates"
```

## 📊 Статус функции

- **URL**: https://functions.poehali.dev/6a33fdc5-867a-4789-92e3-561eb84d9f05
- **Runtime**: Python 3.11
- **Тесты**: 3/3 пройдено ✅

## ⚠️ Известные проблемы

### Ошибка 402 Payment Required
Если функция возвращает 402, это может означать:
1. Превышен лимит бесплатных запросов Cloud Functions
2. Требуется оплата тарифа
3. Проблема с биллингом проекта

**Решение**: Проверьте настройки биллинга в Yandex Cloud или обратитесь в поддержку.

### Webhook не работает
1. Убедитесь, что URL доступен извне
2. Проверьте, что токен бота корректный
3. Проверьте логи функции: `curl <FUNCTION_URL>` должен вернуть 200 OK

## 📝 Структура файлов

```
backend/telegram-bot/
├── index.py           # Основной код бота
├── tests.json         # Тесты функции
├── setup-webhook.py   # Скрипт настройки webhook
└── README.md          # Эта инструкция
```

## 🔗 Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Yandex Cloud Functions](https://cloud.yandex.ru/docs/functions/)
- [Документация проекта](https://vizi-stickers.ru)
